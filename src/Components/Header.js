import React, { Component } from 'react'

const Header = (props) => {
  return(
    <div className="header">
      <div className="container">
        
      </div>
    </div>

  )
}

export default Header;
